import styled from '@emotion/styled'

export const StyledExpandInput = styled.div`
  position: relative;
  height: 32px;
`

export const StyledExpandIcon = styled.span`
  position: absolute;
  top: 6px;
  right: 10px;
  cursor: pointer;
  z-index: 5;
`
